name: Build ARM AppImage

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Pozwala na ręczne uruchomienie

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: |
        # Próbuj zainstalować wszystkie dependencies
        npm install --legacy-peer-deps --force || true
        # Jeśli fail, spróbuj bez optional
        npm install --omit=optional --legacy-peer-deps --force || true
      
    - name: Install Linux-only dependencies
      run: |
        # Te dependencies działają tylko na Linux
        npm install socketcan@4.0.5 --save-optional --no-package-lock || true
        npm install socketmost@2.0.6 --save-optional --no-package-lock || true
        
    - name: Build application
      run: |
        echo "Node version:"
        node --version
        echo "NPM version:"
        npm --version  
        echo "Building application..."
        npm run build
        
    - name: Build ARM64 AppImage
      run: |
        echo "Building ARM64 AppImage..."
        npm run build:armLinux
        echo "Build completed, checking files:"
        ls -la dist/ || echo "No dist directory found"
        
    - name: Upload AppImage
      uses: actions/upload-artifact@v4
      with:
        name: react-carplay-arm64
        path: dist/*.AppImage
        
    - name: Create Release (if tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*.AppImage
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
